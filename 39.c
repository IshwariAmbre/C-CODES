//prime number
//check prime

#include<stdio.h>
#include<stdbool.h>
bool CheckPrime(int iNo)
{
    int iNo= 0;
    int iCnt=0;
    for()

    if(iNo%iCnt==0)
   
}
int main()
{
    int iValue=0;
    bool bRet;

    printf("enter num\n");
    scanf("%d",&iValue);

    bool bRet= CheckPrime(iValue);
    
    if(bRet== true);
    {
         printf("%d num is prime\n", iValue);
    }
    else 
    {
        printf("%d number is not prime\n", iValue);
    }
    return 0;
}